npm install axios framer-motion zustand lucide-react

| Package       | Purpose          |
| ------------- | ---------------- |
| axios         | API requests     |
| framer-motion | animations       |
| zustand       | state management |
| lucide-react  | icons            |



python -m venv venv
venv\Scripts\activate


pip install fastapi uvicorn groq chromadb pymongo python-dotenv
| Package  | Purpose        |
| -------- | -------------- |
| fastapi  | backend API    |
| uvicorn  | server         |
| groq     | AI integration |
| chromadb | vector memory  |
| pymongo  | MongoDB        |
| dotenv   | env variables  |


uvicorn main:app --reload






{
  "character": {
    "name": "Aisha",
    "age": "22",
    "gender": "Female",
    "height": "5'6",
    "hair": "Black wavy hair",
    "eyes": "Brown",
    "skinTone": "Warm beige",
    "clothing": "Oversized hoodie",
    "personality": "Emotionally distant but caring",
    "voice": "Soft and calm",
    "relationship": "Old friend"
  },
  "scene": "A rainy reunion after years apart in a bookstore."
}





WHAT WE BUILD NEXT
PHASE 3 — MEMORY ENGINE
STEP 1 — CREATE MEMORY FOLDER

Inside:

ai-backend/

Create:

memory/
STEP 2 — CREATE MEMORY FILE

Create:

memory/memory_store.py

Paste:

memories = []

This is temporary in-memory storage.

Later:
we upgrade to ChromaDB.

STEP 3 — CREATE MEMORY FUNCTIONS

Replace file contents with:

memories = []

def add_memory(memory):

    memories.append(memory)

def get_memories():

    return memories
STEP 4 — IMPORT MEMORY SYSTEM

Open:

main.py

Add:

from memory.memory_store import add_memory, get_memories
STEP 5 — UPDATE SCENE ENDPOINT

Inside:

generate_scene()

ADD:

past_memories = get_memories()


STORE MEMORIES AFTER GENERATION

AFTER:

completion = client.chat.completions.create(...)

ADD:

add_memory(scene)
FINAL FLOW

Now:

Scene
   ↓
Stored in memory
   ↓
Future scenes retrieve memory
   ↓
AI references past emotionally
STEP 7 — TEST IT
FIRST SCENE
I confessed my feelings to Aisha during a rainy night.

Generate.

SECOND SCENE
Months later we meet again in a quiet café.
EXPECTED RESULT

AI should reference:

confession
emotional tension
awkwardness





28may

| Tool                  | Purpose                    |
| --------------------- | -------------------------- |
| ChromaDB              | vector database            |
| sentence-transformers | embeddings                 |
| FastAPI               | backend                    |
| Groq                  | generation                 |
| MongoDB               | persistent project storage |


Generate Scene
      ↓
Store Embedding
      ↓
New Scene
      ↓
Semantic Retrieval
      ↓
Relevant Memories Injected




emotions are directional different for both person involved



so currently emotions aree getting updated globally like for each relation one things but in real world it does not work like that i want that to be updated seperately for both characters like if a scene involve 3 people A B C then relationshipbetween each pair should be differrent and a to b and b to a should be considred different i have given relationships.py in data folder extend that logic also impprove the prompt and also on scene generation page display the updated score and on canvas give the option to updated emotions for both characetsrs indiviadia;;y not common update in relationship


| Tool                      | Purpose          |
| ------------------------- | ---------------- |
| HuggingFace Inference API | image generation |
| FLUX / SDXL               | model            |
| FastAPI                   | orchestration    |
| Director AI               | visual guidance  |
